@inherits LayoutComponentBase
@inject NavigationManager NavManager

<div class="navbar">
    <NavLink href="/" Match="NavLinkMatch.All">Accueil</NavLink>
    <NavLink href="/checkout" Match="NavLinkMatch.All">Valider ma commande</NavLink>
</div>

<div class="content">
    @Body
</div>

<style>
    .navbar {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        height: 60px;
        display: flex;
        justify-content: center; /* centre horizontalement */
        align-items: center; /* centre verticalement */
        gap: 2rem; /* espace entre les liens */
        padding: 0 1rem;
        background-color: #f5f5f5;
        z-index: 1000;
    }

    .content {
        padding-top: 60px; /* même hauteur que .navbar */
        padding: 2rem 1rem;
    }

    .navbar .active {
        font-weight: bold;
        text-decoration: underline;
    }
</style>
